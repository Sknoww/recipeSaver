import React from "react";
import MainContainer from "../components/bundles/containers/MainContainer";

const Settings = () => {
    return (
        <React.Fragment>
            <main className="container">
                <MainContainer header="Settings" />
            </main>
        </React.Fragment>
    );
};

export default Settings;
